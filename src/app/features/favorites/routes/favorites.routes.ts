import type { Routes } from '@angular/router';

import { FavoritesPageComponent } from '../pages/favorites-page.component';

export const FAVORITES_ROUTES: Routes = [{ path: '', component: FavoritesPageComponent }];
