import type { Routes } from '@angular/router';

import { AccountPageComponent } from '../pages/account-page.component';

export const ACCOUNT_ROUTES: Routes = [{ path: '', component: AccountPageComponent }];
